# Antigravity Context Loading

## Instructions for Antigravity

1. **Mandatory SSOT Load**: IMMEDIATELY load and review `./openspec/config.yaml`, `./openspec/CONVENTIONS.md`, and `./AGENTS_SSYNC_RULES.md` before proceeding with any user request.
2. **Shared Skills**: Your skills are located as symlinks in `./.agent/skills/` pointing to `../../skills/shared/`. Always refer to these shared paths if modifying skill definitions.
3. **OpenSpec Sync Requirement**: You MUST keep the `./openspec/` directory synchronized with all project changes. When you complete a task, ensure that the open specs or active changes are updated. Never leave the project in a state where the code diverges from OpenSpec.
